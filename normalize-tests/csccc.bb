(def main (params) i32
  (let ignore
    (call first (types i32) i32 (args
      (call i (types i32) i32 (args
        (call i (types i32) i32 (args
        (call i (types i32) i32 (args
        (call i (types i32) i32 (args 4))))))))
      (call i (types i32) i32 (args
        (call i (types i32) i32 (args
        (call i (types i32) i32 (args
        (call i (types i32) i32 (args 4))))))))
    )))
  (return 0 i32))